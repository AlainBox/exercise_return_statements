const calculateAverage = function(a, b, c, d, e){
    return((a + b + c + d + e) / 5);
};

console.log(Math.round(calculateAverage(2, 4, 6, 8, 10)));
console.log(Math.round(calculateAverage(2.6, 4.8, 6.3, 8.1, 10.9)));
console.log(Math.round(calculateAverage(3.9, 45, 2, 19.6, 21)));
console.log(Math.round(calculateAverage(2, 90.6, 50, 23.2, 13)));

// This is a function that produces something.
